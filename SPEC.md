# まるつけ — 仕様

デザインプロトタイプ（`docs/index.html`）で確定した内容。バックエンド実装の基準はこの文書とする。
UIの見た目と操作はプロトタイプ本体、設計判断の理由はプロトタイプ下部の「DESIGN DECISIONS」を参照。

## 前提

- 1コマ最大2名の個別指導。講師がスマホ／タブレットを片手に持ったまま採点する
- 2名は**別々の教材**を進める。作業位置（教材・ページ・見出し・大問・小問）は生徒ごとに保持する
- 塾の講師複数で共有する。教室の通信環境は安定しているのでオンライン前提でよい
- 記録は授業中にリアルタイムで入力する。1問あたりのタップ数を増やさないことが最優先

## 問題の同一性

**1つの問題を指すのは次の5つの組。どれを欠いても別々の問題が同じ記録に潰れる。**

| 要素 | 型 | 例 | 備考 |
|---|---|---|---|
| 教材 | ID | `b1` | 教材が違えば同じページ番号でも無関係 |
| ページ | 整数 | `78` | |
| 見出し | 文字列 | `基本問題` / `""` | 空文字＝見出しなし |
| 大問 | 整数 | `2` | 表示は四角囲み |
| 小問 | 整数 | `3` | 表示は `(3)` |

表示形式は `p.78 基本問題 2(3)`、見出しなしなら `p.78 2(3)`。

### なぜ5つ必要か

- 同じページに `1(1)` と `2(1)` が並ぶ → 大問が要る
- 番号が同じで見出しだけ違う組がある（`確認問題 2(3)` と `練習問題 2(3)`）→ 見出しが要る
- 見出しと番号を1つの文字列にすると「基本問題1」「基本問題2」…を個別に登録することになり、選択肢が際限なく増える → 分けて持つ

## エンティティ

### 生徒 student
`id` / `name` / （将来）学年・在籍状態

### 教材 textbook
`id` / `title` / `short`（略称・一覧表示用） / `subject` / `pages`（総ページ数）

単元と見出しの語彙は**教材に属する**。教材をまたいで共有しない。

### 単元 unit
`textbook_id` / `name` / `page_start` / `page_end`

記録には単元を持たせない。ページ範囲から引くだけ。あとから区切りを直せば過去の集計も一緒に直る。
範囲外のページは「単元 未登録」として扱い、分析に出さない。

### 見出しの語彙 section
`textbook_id` / `label`

教材ごとに数個。授業中に追加でき、以後はチップとして選ぶだけ。番号は含めない（番号は大問フィールド）。

### 記録 record

| 列 | 型 | 備考 |
|---|---|---|
| id | | |
| student_id | | |
| textbook_id | | |
| page | 整数 | |
| section | 文字列 | 空文字可 |
| major | 整数 | 大問 |
| minor | 整数 | 小問 |
| result | 1 / 2 / 3 | 1=○正解 2=×不正解 3=−未実施 |
| cause | 0 / 1 / 2 / 3 | resultが2のときだけ意味を持つ。0=未分類 |
| created_at | 日時 | 授業日はここから導出する |
| created_by | 講師 | |

`cause`（つまずきの分類）

| 値 | 呼称 | 意味 | 色 |
|---|---|---|---|
| 1 | できる | 知識があり解ける（ミス） | 黄 |
| 2 | あいまい | 知識があいまいで解けない | 緑 |
| 3 | 知識なし | 知識がなく解けない | 赤 |

## 導出ルール

**状態を持つ列は作らない。すべて記録から導出する。**

### 復習リスト
同じ問題（上記5つの組）の**最新の記録**を見る。

- `result = 2` → 「間違えた問題」
- `result = 3` → 「未実施」
- `result = 1` → どちらからも外れる（1回で卒業）

過去の × は消えずに全部残り、正答率の計算にも使われる。
並び順は cause の降順（知識なし→あいまい→できる→未分類）、次に誤答回数の降順、次に古い順。

### 正答率
`○ ÷ (○ + ×)`。**未実施は母数に入れない。**
入れると、飛ばした問題が増えるほど正答率が下がり、できるようになったのか手をつけていないだけなのか読めなくなる。

### データ不足
実施した問題（○+×）が3問未満の単元は、正答率で並べ替えず「データ不足」として下に置く。
未実施しかない単元は正答率を `—` にする。

### 授業日
`created_at` の日付でまとめるだけ。「授業日」テーブルは作らない。
作ると、記録のない日が残ったり記録した日が抜けたりして実態とずれる。

### 単元別の集計
単元名は教材ごとの語彙なので、**同じ名前でも教材をまたいで合算しない**。
（2冊とも「式の計算」を持つが、中2と中3では範囲も難度も違う）

## 入力時のふるまい

- 採点後、選択中の生徒・教材・見出し・大問は動かず、**小問だけ +1**
- 大問を送る → 小問が (1) に戻る
- 見出しを変える → 大問・小問が 1(1) に戻る
- ページを送る → 大問・小問が 1(1) に戻る。**見出しは維持**（見出しは複数ページにまたがるため）
- 教材を変える → その教材の最初の登録ページへ。見出し・大問・小問はリセット
- 生徒を切り替える → その生徒の作業位置を復元
- 確認ダイアログは出さない。記録は即確定し、直後のトーストで取り消せる
- × のときだけ、トーストでつまずきの分類を聞く。押さなければ未分類のまま進む

## 表示上の決まり

- 数字はすべて等幅（tabular-nums）
- 大問の四角囲みはCSSの枠で描く。囲み数字の文字（①②）は10以上で崩れ、桁送りからも外れる
- 教材名は、その生徒が2冊以上使っているときだけ添える（1冊なら情報量がゼロ）
- 色だけに意味を持たせない。○×− と つまずき3分類は必ず記号か語と組で出す

### 配色

| 用途 | ライト | ダーク |
|---|---|---|
| ○ 正解 | `#1A6E9E` | `#3596C6` |
| × 不正解 | `#C13A5C` | `#E05570` |
| − 未実施 | `#8E95A9` | `#6E7692` |
| できる | `#C29100` | `#C6A02A` |
| あいまい | `#16855E` | `#31A06B` |
| 知識なし | `#C4342F` | `#DE5A52` |
| 操作色（藍） | `#2F4B8F` | `#7D9BE2` |

○×− の2色は色覚特性下での分離を実測して決めた（緑と紅は区別できないため ○ を藍青にした）。

**未解決:** つまずき3分類の緑と赤は、色覚特性下で ΔE 2.6（基準8）。ユーザー指定の色なのでそのまま使い、
凡例に件数を添える・緑にハッチを敷く等で情報は落ちないようにしてある。判別速度は落ちる。

## 未着手

- ログイン、生徒の登録・編集、授業の開始／終了、教材の新規登録フォーム
- バックエンド（GAS + スプレッドシート）
